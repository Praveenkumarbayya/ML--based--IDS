[pytest]
testpaths = tests
addopts = -ra
markers =
    integration: end-to-end integration tests (slower; run with `pytest -k integration`)
filterwarnings =
    ignore::DeprecationWarning
    ignore::PendingDeprecationWarning
    ignore::UserWarning:lightgbm
    ignore::UserWarning:sklearn
